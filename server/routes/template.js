// change name of file based on route

const express = require('express');
// import controllers for middleware
const router = express.Router();

// router.get('/', someController.stuff, (req, res) => {});

module.exports = router;
